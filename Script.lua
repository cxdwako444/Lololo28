-- WAKO Visual-Heavy GUI Project


-- Decorative Panel 0
local panel0 = Instance.new("Frame")
panel0.Size = UDim2.new(0, 100 + 0 * 2, 0, 20)
panel0.Position = UDim2.new(0, 0, 0, 0)
panel0.BackgroundColor3 = Color3.fromRGB(100, 150, 255)
panel0.BorderSizePixel = 0
panel0.Parent = main

local panelCorner0 = Instance.new("UICorner")
panelCorner0.CornerRadius = UDim.new(0, 6)
panelCorner0.Parent = panel0

-- Animation for Panel 0
TweenService:Create(panel0, TweenInfo.new(1 + 0 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 0 * 2, 0, 30)
}):Play()

-- Decorative Panel 1
local panel1 = Instance.new("Frame")
panel1.Size = UDim2.new(0, 100 + 1 * 2, 0, 20)
panel1.Position = UDim2.new(0, 4, 0, 6)
panel1.BackgroundColor3 = Color3.fromRGB(103, 152, 255)
panel1.BorderSizePixel = 0
panel1.Parent = main

local panelCorner1 = Instance.new("UICorner")
panelCorner1.CornerRadius = UDim.new(0, 6)
panelCorner1.Parent = panel1

-- Animation for Panel 1
TweenService:Create(panel1, TweenInfo.new(1 + 1 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 1 * 2, 0, 30)
}):Play()

-- Decorative Panel 2
local panel2 = Instance.new("Frame")
panel2.Size = UDim2.new(0, 100 + 2 * 2, 0, 20)
panel2.Position = UDim2.new(0, 8, 0, 12)
panel2.BackgroundColor3 = Color3.fromRGB(106, 154, 255)
panel2.BorderSizePixel = 0
panel2.Parent = main

local panelCorner2 = Instance.new("UICorner")
panelCorner2.CornerRadius = UDim.new(0, 6)
panelCorner2.Parent = panel2

-- Animation for Panel 2
TweenService:Create(panel2, TweenInfo.new(1 + 2 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 2 * 2, 0, 30)
}):Play()

-- Decorative Panel 3
local panel3 = Instance.new("Frame")
panel3.Size = UDim2.new(0, 100 + 3 * 2, 0, 20)
panel3.Position = UDim2.new(0, 12, 0, 18)
panel3.BackgroundColor3 = Color3.fromRGB(109, 156, 255)
panel3.BorderSizePixel = 0
panel3.Parent = main

local panelCorner3 = Instance.new("UICorner")
panelCorner3.CornerRadius = UDim.new(0, 6)
panelCorner3.Parent = panel3

-- Animation for Panel 3
TweenService:Create(panel3, TweenInfo.new(1 + 3 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 3 * 2, 0, 30)
}):Play()

-- Decorative Panel 4
local panel4 = Instance.new("Frame")
panel4.Size = UDim2.new(0, 100 + 4 * 2, 0, 20)
panel4.Position = UDim2.new(0, 16, 0, 24)
panel4.BackgroundColor3 = Color3.fromRGB(112, 158, 255)
panel4.BorderSizePixel = 0
panel4.Parent = main

local panelCorner4 = Instance.new("UICorner")
panelCorner4.CornerRadius = UDim.new(0, 6)
panelCorner4.Parent = panel4

-- Animation for Panel 4
TweenService:Create(panel4, TweenInfo.new(1 + 4 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 4 * 2, 0, 30)
}):Play()

-- Decorative Panel 5
local panel5 = Instance.new("Frame")
panel5.Size = UDim2.new(0, 100 + 5 * 2, 0, 20)
panel5.Position = UDim2.new(0, 20, 0, 30)
panel5.BackgroundColor3 = Color3.fromRGB(115, 160, 255)
panel5.BorderSizePixel = 0
panel5.Parent = main

local panelCorner5 = Instance.new("UICorner")
panelCorner5.CornerRadius = UDim.new(0, 6)
panelCorner5.Parent = panel5

-- Animation for Panel 5
TweenService:Create(panel5, TweenInfo.new(1 + 5 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 5 * 2, 0, 30)
}):Play()

-- Decorative Panel 6
local panel6 = Instance.new("Frame")
panel6.Size = UDim2.new(0, 100 + 6 * 2, 0, 20)
panel6.Position = UDim2.new(0, 24, 0, 36)
panel6.BackgroundColor3 = Color3.fromRGB(118, 162, 255)
panel6.BorderSizePixel = 0
panel6.Parent = main

local panelCorner6 = Instance.new("UICorner")
panelCorner6.CornerRadius = UDim.new(0, 6)
panelCorner6.Parent = panel6

-- Animation for Panel 6
TweenService:Create(panel6, TweenInfo.new(1 + 6 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 6 * 2, 0, 30)
}):Play()

-- Decorative Panel 7
local panel7 = Instance.new("Frame")
panel7.Size = UDim2.new(0, 100 + 7 * 2, 0, 20)
panel7.Position = UDim2.new(0, 28, 0, 42)
panel7.BackgroundColor3 = Color3.fromRGB(121, 164, 255)
panel7.BorderSizePixel = 0
panel7.Parent = main

local panelCorner7 = Instance.new("UICorner")
panelCorner7.CornerRadius = UDim.new(0, 6)
panelCorner7.Parent = panel7

-- Animation for Panel 7
TweenService:Create(panel7, TweenInfo.new(1 + 7 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 7 * 2, 0, 30)
}):Play()

-- Decorative Panel 8
local panel8 = Instance.new("Frame")
panel8.Size = UDim2.new(0, 100 + 8 * 2, 0, 20)
panel8.Position = UDim2.new(0, 32, 0, 48)
panel8.BackgroundColor3 = Color3.fromRGB(124, 166, 255)
panel8.BorderSizePixel = 0
panel8.Parent = main

local panelCorner8 = Instance.new("UICorner")
panelCorner8.CornerRadius = UDim.new(0, 6)
panelCorner8.Parent = panel8

-- Animation for Panel 8
TweenService:Create(panel8, TweenInfo.new(1 + 8 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 8 * 2, 0, 30)
}):Play()

-- Decorative Panel 9
local panel9 = Instance.new("Frame")
panel9.Size = UDim2.new(0, 100 + 9 * 2, 0, 20)
panel9.Position = UDim2.new(0, 36, 0, 54)
panel9.BackgroundColor3 = Color3.fromRGB(127, 168, 255)
panel9.BorderSizePixel = 0
panel9.Parent = main

local panelCorner9 = Instance.new("UICorner")
panelCorner9.CornerRadius = UDim.new(0, 6)
panelCorner9.Parent = panel9

-- Animation for Panel 9
TweenService:Create(panel9, TweenInfo.new(1 + 9 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 9 * 2, 0, 30)
}):Play()

-- Decorative Panel 10
local panel10 = Instance.new("Frame")
panel10.Size = UDim2.new(0, 100 + 10 * 2, 0, 20)
panel10.Position = UDim2.new(0, 40, 0, 60)
panel10.BackgroundColor3 = Color3.fromRGB(130, 170, 255)
panel10.BorderSizePixel = 0
panel10.Parent = main

local panelCorner10 = Instance.new("UICorner")
panelCorner10.CornerRadius = UDim.new(0, 6)
panelCorner10.Parent = panel10

-- Animation for Panel 10
TweenService:Create(panel10, TweenInfo.new(1 + 10 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 10 * 2, 0, 30)
}):Play()

-- Decorative Panel 11
local panel11 = Instance.new("Frame")
panel11.Size = UDim2.new(0, 100 + 11 * 2, 0, 20)
panel11.Position = UDim2.new(0, 44, 0, 66)
panel11.BackgroundColor3 = Color3.fromRGB(133, 172, 255)
panel11.BorderSizePixel = 0
panel11.Parent = main

local panelCorner11 = Instance.new("UICorner")
panelCorner11.CornerRadius = UDim.new(0, 6)
panelCorner11.Parent = panel11

-- Animation for Panel 11
TweenService:Create(panel11, TweenInfo.new(1 + 11 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 11 * 2, 0, 30)
}):Play()

-- Decorative Panel 12
local panel12 = Instance.new("Frame")
panel12.Size = UDim2.new(0, 100 + 12 * 2, 0, 20)
panel12.Position = UDim2.new(0, 48, 0, 72)
panel12.BackgroundColor3 = Color3.fromRGB(136, 174, 255)
panel12.BorderSizePixel = 0
panel12.Parent = main

local panelCorner12 = Instance.new("UICorner")
panelCorner12.CornerRadius = UDim.new(0, 6)
panelCorner12.Parent = panel12

-- Animation for Panel 12
TweenService:Create(panel12, TweenInfo.new(1 + 12 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 12 * 2, 0, 30)
}):Play()

-- Decorative Panel 13
local panel13 = Instance.new("Frame")
panel13.Size = UDim2.new(0, 100 + 13 * 2, 0, 20)
panel13.Position = UDim2.new(0, 52, 0, 78)
panel13.BackgroundColor3 = Color3.fromRGB(139, 176, 255)
panel13.BorderSizePixel = 0
panel13.Parent = main

local panelCorner13 = Instance.new("UICorner")
panelCorner13.CornerRadius = UDim.new(0, 6)
panelCorner13.Parent = panel13

-- Animation for Panel 13
TweenService:Create(panel13, TweenInfo.new(1 + 13 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 13 * 2, 0, 30)
}):Play()

-- Decorative Panel 14
local panel14 = Instance.new("Frame")
panel14.Size = UDim2.new(0, 100 + 14 * 2, 0, 20)
panel14.Position = UDim2.new(0, 56, 0, 84)
panel14.BackgroundColor3 = Color3.fromRGB(142, 178, 255)
panel14.BorderSizePixel = 0
panel14.Parent = main

local panelCorner14 = Instance.new("UICorner")
panelCorner14.CornerRadius = UDim.new(0, 6)
panelCorner14.Parent = panel14

-- Animation for Panel 14
TweenService:Create(panel14, TweenInfo.new(1 + 14 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 14 * 2, 0, 30)
}):Play()

-- Decorative Panel 15
local panel15 = Instance.new("Frame")
panel15.Size = UDim2.new(0, 100 + 15 * 2, 0, 20)
panel15.Position = UDim2.new(0, 60, 0, 90)
panel15.BackgroundColor3 = Color3.fromRGB(145, 180, 255)
panel15.BorderSizePixel = 0
panel15.Parent = main

local panelCorner15 = Instance.new("UICorner")
panelCorner15.CornerRadius = UDim.new(0, 6)
panelCorner15.Parent = panel15

-- Animation for Panel 15
TweenService:Create(panel15, TweenInfo.new(1 + 15 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 15 * 2, 0, 30)
}):Play()

-- Decorative Panel 16
local panel16 = Instance.new("Frame")
panel16.Size = UDim2.new(0, 100 + 16 * 2, 0, 20)
panel16.Position = UDim2.new(0, 64, 0, 96)
panel16.BackgroundColor3 = Color3.fromRGB(148, 182, 255)
panel16.BorderSizePixel = 0
panel16.Parent = main

local panelCorner16 = Instance.new("UICorner")
panelCorner16.CornerRadius = UDim.new(0, 6)
panelCorner16.Parent = panel16

-- Animation for Panel 16
TweenService:Create(panel16, TweenInfo.new(1 + 16 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 16 * 2, 0, 30)
}):Play()

-- Decorative Panel 17
local panel17 = Instance.new("Frame")
panel17.Size = UDim2.new(0, 100 + 17 * 2, 0, 20)
panel17.Position = UDim2.new(0, 68, 0, 102)
panel17.BackgroundColor3 = Color3.fromRGB(151, 184, 255)
panel17.BorderSizePixel = 0
panel17.Parent = main

local panelCorner17 = Instance.new("UICorner")
panelCorner17.CornerRadius = UDim.new(0, 6)
panelCorner17.Parent = panel17

-- Animation for Panel 17
TweenService:Create(panel17, TweenInfo.new(1 + 17 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 17 * 2, 0, 30)
}):Play()

-- Decorative Panel 18
local panel18 = Instance.new("Frame")
panel18.Size = UDim2.new(0, 100 + 18 * 2, 0, 20)
panel18.Position = UDim2.new(0, 72, 0, 108)
panel18.BackgroundColor3 = Color3.fromRGB(154, 186, 255)
panel18.BorderSizePixel = 0
panel18.Parent = main

local panelCorner18 = Instance.new("UICorner")
panelCorner18.CornerRadius = UDim.new(0, 6)
panelCorner18.Parent = panel18

-- Animation for Panel 18
TweenService:Create(panel18, TweenInfo.new(1 + 18 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 18 * 2, 0, 30)
}):Play()

-- Decorative Panel 19
local panel19 = Instance.new("Frame")
panel19.Size = UDim2.new(0, 100 + 19 * 2, 0, 20)
panel19.Position = UDim2.new(0, 76, 0, 114)
panel19.BackgroundColor3 = Color3.fromRGB(157, 188, 255)
panel19.BorderSizePixel = 0
panel19.Parent = main

local panelCorner19 = Instance.new("UICorner")
panelCorner19.CornerRadius = UDim.new(0, 6)
panelCorner19.Parent = panel19

-- Animation for Panel 19
TweenService:Create(panel19, TweenInfo.new(1 + 19 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 19 * 2, 0, 30)
}):Play()

-- Decorative Panel 20
local panel20 = Instance.new("Frame")
panel20.Size = UDim2.new(0, 100 + 20 * 2, 0, 20)
panel20.Position = UDim2.new(0, 80, 0, 120)
panel20.BackgroundColor3 = Color3.fromRGB(160, 190, 255)
panel20.BorderSizePixel = 0
panel20.Parent = main

local panelCorner20 = Instance.new("UICorner")
panelCorner20.CornerRadius = UDim.new(0, 6)
panelCorner20.Parent = panel20

-- Animation for Panel 20
TweenService:Create(panel20, TweenInfo.new(1 + 20 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 20 * 2, 0, 30)
}):Play()

-- Decorative Panel 21
local panel21 = Instance.new("Frame")
panel21.Size = UDim2.new(0, 100 + 21 * 2, 0, 20)
panel21.Position = UDim2.new(0, 84, 0, 126)
panel21.BackgroundColor3 = Color3.fromRGB(163, 192, 255)
panel21.BorderSizePixel = 0
panel21.Parent = main

local panelCorner21 = Instance.new("UICorner")
panelCorner21.CornerRadius = UDim.new(0, 6)
panelCorner21.Parent = panel21

-- Animation for Panel 21
TweenService:Create(panel21, TweenInfo.new(1 + 21 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 21 * 2, 0, 30)
}):Play()

-- Decorative Panel 22
local panel22 = Instance.new("Frame")
panel22.Size = UDim2.new(0, 100 + 22 * 2, 0, 20)
panel22.Position = UDim2.new(0, 88, 0, 132)
panel22.BackgroundColor3 = Color3.fromRGB(166, 194, 255)
panel22.BorderSizePixel = 0
panel22.Parent = main

local panelCorner22 = Instance.new("UICorner")
panelCorner22.CornerRadius = UDim.new(0, 6)
panelCorner22.Parent = panel22

-- Animation for Panel 22
TweenService:Create(panel22, TweenInfo.new(1 + 22 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 22 * 2, 0, 30)
}):Play()

-- Decorative Panel 23
local panel23 = Instance.new("Frame")
panel23.Size = UDim2.new(0, 100 + 23 * 2, 0, 20)
panel23.Position = UDim2.new(0, 92, 0, 138)
panel23.BackgroundColor3 = Color3.fromRGB(169, 196, 255)
panel23.BorderSizePixel = 0
panel23.Parent = main

local panelCorner23 = Instance.new("UICorner")
panelCorner23.CornerRadius = UDim.new(0, 6)
panelCorner23.Parent = panel23

-- Animation for Panel 23
TweenService:Create(panel23, TweenInfo.new(1 + 23 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 23 * 2, 0, 30)
}):Play()

-- Decorative Panel 24
local panel24 = Instance.new("Frame")
panel24.Size = UDim2.new(0, 100 + 24 * 2, 0, 20)
panel24.Position = UDim2.new(0, 96, 0, 144)
panel24.BackgroundColor3 = Color3.fromRGB(172, 198, 255)
panel24.BorderSizePixel = 0
panel24.Parent = main

local panelCorner24 = Instance.new("UICorner")
panelCorner24.CornerRadius = UDim.new(0, 6)
panelCorner24.Parent = panel24

-- Animation for Panel 24
TweenService:Create(panel24, TweenInfo.new(1 + 24 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 24 * 2, 0, 30)
}):Play()

-- Decorative Panel 25
local panel25 = Instance.new("Frame")
panel25.Size = UDim2.new(0, 100 + 25 * 2, 0, 20)
panel25.Position = UDim2.new(0, 100, 0, 150)
panel25.BackgroundColor3 = Color3.fromRGB(175, 200, 255)
panel25.BorderSizePixel = 0
panel25.Parent = main

local panelCorner25 = Instance.new("UICorner")
panelCorner25.CornerRadius = UDim.new(0, 6)
panelCorner25.Parent = panel25

-- Animation for Panel 25
TweenService:Create(panel25, TweenInfo.new(1 + 25 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 25 * 2, 0, 30)
}):Play()

-- Decorative Panel 26
local panel26 = Instance.new("Frame")
panel26.Size = UDim2.new(0, 100 + 26 * 2, 0, 20)
panel26.Position = UDim2.new(0, 104, 0, 156)
panel26.BackgroundColor3 = Color3.fromRGB(178, 202, 255)
panel26.BorderSizePixel = 0
panel26.Parent = main

local panelCorner26 = Instance.new("UICorner")
panelCorner26.CornerRadius = UDim.new(0, 6)
panelCorner26.Parent = panel26

-- Animation for Panel 26
TweenService:Create(panel26, TweenInfo.new(1 + 26 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 26 * 2, 0, 30)
}):Play()

-- Decorative Panel 27
local panel27 = Instance.new("Frame")
panel27.Size = UDim2.new(0, 100 + 27 * 2, 0, 20)
panel27.Position = UDim2.new(0, 108, 0, 162)
panel27.BackgroundColor3 = Color3.fromRGB(181, 204, 255)
panel27.BorderSizePixel = 0
panel27.Parent = main

local panelCorner27 = Instance.new("UICorner")
panelCorner27.CornerRadius = UDim.new(0, 6)
panelCorner27.Parent = panel27

-- Animation for Panel 27
TweenService:Create(panel27, TweenInfo.new(1 + 27 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 27 * 2, 0, 30)
}):Play()

-- Decorative Panel 28
local panel28 = Instance.new("Frame")
panel28.Size = UDim2.new(0, 100 + 28 * 2, 0, 20)
panel28.Position = UDim2.new(0, 112, 0, 168)
panel28.BackgroundColor3 = Color3.fromRGB(184, 206, 255)
panel28.BorderSizePixel = 0
panel28.Parent = main

local panelCorner28 = Instance.new("UICorner")
panelCorner28.CornerRadius = UDim.new(0, 6)
panelCorner28.Parent = panel28

-- Animation for Panel 28
TweenService:Create(panel28, TweenInfo.new(1 + 28 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 28 * 2, 0, 30)
}):Play()

-- Decorative Panel 29
local panel29 = Instance.new("Frame")
panel29.Size = UDim2.new(0, 100 + 29 * 2, 0, 20)
panel29.Position = UDim2.new(0, 116, 0, 174)
panel29.BackgroundColor3 = Color3.fromRGB(187, 208, 255)
panel29.BorderSizePixel = 0
panel29.Parent = main

local panelCorner29 = Instance.new("UICorner")
panelCorner29.CornerRadius = UDim.new(0, 6)
panelCorner29.Parent = panel29

-- Animation for Panel 29
TweenService:Create(panel29, TweenInfo.new(1 + 29 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 29 * 2, 0, 30)
}):Play()

-- Decorative Panel 30
local panel30 = Instance.new("Frame")
panel30.Size = UDim2.new(0, 100 + 30 * 2, 0, 20)
panel30.Position = UDim2.new(0, 120, 0, 180)
panel30.BackgroundColor3 = Color3.fromRGB(190, 210, 255)
panel30.BorderSizePixel = 0
panel30.Parent = main

local panelCorner30 = Instance.new("UICorner")
panelCorner30.CornerRadius = UDim.new(0, 6)
panelCorner30.Parent = panel30

-- Animation for Panel 30
TweenService:Create(panel30, TweenInfo.new(1 + 30 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 30 * 2, 0, 30)
}):Play()

-- Decorative Panel 31
local panel31 = Instance.new("Frame")
panel31.Size = UDim2.new(0, 100 + 31 * 2, 0, 20)
panel31.Position = UDim2.new(0, 124, 0, 186)
panel31.BackgroundColor3 = Color3.fromRGB(193, 212, 255)
panel31.BorderSizePixel = 0
panel31.Parent = main

local panelCorner31 = Instance.new("UICorner")
panelCorner31.CornerRadius = UDim.new(0, 6)
panelCorner31.Parent = panel31

-- Animation for Panel 31
TweenService:Create(panel31, TweenInfo.new(1 + 31 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 31 * 2, 0, 30)
}):Play()

-- Decorative Panel 32
local panel32 = Instance.new("Frame")
panel32.Size = UDim2.new(0, 100 + 32 * 2, 0, 20)
panel32.Position = UDim2.new(0, 128, 0, 192)
panel32.BackgroundColor3 = Color3.fromRGB(196, 214, 255)
panel32.BorderSizePixel = 0
panel32.Parent = main

local panelCorner32 = Instance.new("UICorner")
panelCorner32.CornerRadius = UDim.new(0, 6)
panelCorner32.Parent = panel32

-- Animation for Panel 32
TweenService:Create(panel32, TweenInfo.new(1 + 32 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 32 * 2, 0, 30)
}):Play()

-- Decorative Panel 33
local panel33 = Instance.new("Frame")
panel33.Size = UDim2.new(0, 100 + 33 * 2, 0, 20)
panel33.Position = UDim2.new(0, 132, 0, 198)
panel33.BackgroundColor3 = Color3.fromRGB(199, 216, 255)
panel33.BorderSizePixel = 0
panel33.Parent = main

local panelCorner33 = Instance.new("UICorner")
panelCorner33.CornerRadius = UDim.new(0, 6)
panelCorner33.Parent = panel33

-- Animation for Panel 33
TweenService:Create(panel33, TweenInfo.new(1 + 33 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 33 * 2, 0, 30)
}):Play()

-- Decorative Panel 34
local panel34 = Instance.new("Frame")
panel34.Size = UDim2.new(0, 100 + 34 * 2, 0, 20)
panel34.Position = UDim2.new(0, 136, 0, 204)
panel34.BackgroundColor3 = Color3.fromRGB(202, 218, 255)
panel34.BorderSizePixel = 0
panel34.Parent = main

local panelCorner34 = Instance.new("UICorner")
panelCorner34.CornerRadius = UDim.new(0, 6)
panelCorner34.Parent = panel34

-- Animation for Panel 34
TweenService:Create(panel34, TweenInfo.new(1 + 34 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 34 * 2, 0, 30)
}):Play()

-- Decorative Panel 35
local panel35 = Instance.new("Frame")
panel35.Size = UDim2.new(0, 100 + 35 * 2, 0, 20)
panel35.Position = UDim2.new(0, 140, 0, 210)
panel35.BackgroundColor3 = Color3.fromRGB(205, 220, 255)
panel35.BorderSizePixel = 0
panel35.Parent = main

local panelCorner35 = Instance.new("UICorner")
panelCorner35.CornerRadius = UDim.new(0, 6)
panelCorner35.Parent = panel35

-- Animation for Panel 35
TweenService:Create(panel35, TweenInfo.new(1 + 35 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 35 * 2, 0, 30)
}):Play()

-- Decorative Panel 36
local panel36 = Instance.new("Frame")
panel36.Size = UDim2.new(0, 100 + 36 * 2, 0, 20)
panel36.Position = UDim2.new(0, 144, 0, 216)
panel36.BackgroundColor3 = Color3.fromRGB(208, 222, 255)
panel36.BorderSizePixel = 0
panel36.Parent = main

local panelCorner36 = Instance.new("UICorner")
panelCorner36.CornerRadius = UDim.new(0, 6)
panelCorner36.Parent = panel36

-- Animation for Panel 36
TweenService:Create(panel36, TweenInfo.new(1 + 36 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 36 * 2, 0, 30)
}):Play()

-- Decorative Panel 37
local panel37 = Instance.new("Frame")
panel37.Size = UDim2.new(0, 100 + 37 * 2, 0, 20)
panel37.Position = UDim2.new(0, 148, 0, 222)
panel37.BackgroundColor3 = Color3.fromRGB(211, 224, 255)
panel37.BorderSizePixel = 0
panel37.Parent = main

local panelCorner37 = Instance.new("UICorner")
panelCorner37.CornerRadius = UDim.new(0, 6)
panelCorner37.Parent = panel37

-- Animation for Panel 37
TweenService:Create(panel37, TweenInfo.new(1 + 37 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 37 * 2, 0, 30)
}):Play()

-- Decorative Panel 38
local panel38 = Instance.new("Frame")
panel38.Size = UDim2.new(0, 100 + 38 * 2, 0, 20)
panel38.Position = UDim2.new(0, 152, 0, 228)
panel38.BackgroundColor3 = Color3.fromRGB(214, 226, 255)
panel38.BorderSizePixel = 0
panel38.Parent = main

local panelCorner38 = Instance.new("UICorner")
panelCorner38.CornerRadius = UDim.new(0, 6)
panelCorner38.Parent = panel38

-- Animation for Panel 38
TweenService:Create(panel38, TweenInfo.new(1 + 38 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 38 * 2, 0, 30)
}):Play()

-- Decorative Panel 39
local panel39 = Instance.new("Frame")
panel39.Size = UDim2.new(0, 100 + 39 * 2, 0, 20)
panel39.Position = UDim2.new(0, 156, 0, 234)
panel39.BackgroundColor3 = Color3.fromRGB(217, 228, 255)
panel39.BorderSizePixel = 0
panel39.Parent = main

local panelCorner39 = Instance.new("UICorner")
panelCorner39.CornerRadius = UDim.new(0, 6)
panelCorner39.Parent = panel39

-- Animation for Panel 39
TweenService:Create(panel39, TweenInfo.new(1 + 39 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 39 * 2, 0, 30)
}):Play()

-- Decorative Panel 40
local panel40 = Instance.new("Frame")
panel40.Size = UDim2.new(0, 100 + 40 * 2, 0, 20)
panel40.Position = UDim2.new(0, 160, 0, 240)
panel40.BackgroundColor3 = Color3.fromRGB(220, 230, 255)
panel40.BorderSizePixel = 0
panel40.Parent = main

local panelCorner40 = Instance.new("UICorner")
panelCorner40.CornerRadius = UDim.new(0, 6)
panelCorner40.Parent = panel40

-- Animation for Panel 40
TweenService:Create(panel40, TweenInfo.new(1 + 40 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 40 * 2, 0, 30)
}):Play()

-- Decorative Panel 41
local panel41 = Instance.new("Frame")
panel41.Size = UDim2.new(0, 100 + 41 * 2, 0, 20)
panel41.Position = UDim2.new(0, 164, 0, 246)
panel41.BackgroundColor3 = Color3.fromRGB(223, 232, 255)
panel41.BorderSizePixel = 0
panel41.Parent = main

local panelCorner41 = Instance.new("UICorner")
panelCorner41.CornerRadius = UDim.new(0, 6)
panelCorner41.Parent = panel41

-- Animation for Panel 41
TweenService:Create(panel41, TweenInfo.new(1 + 41 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 41 * 2, 0, 30)
}):Play()

-- Decorative Panel 42
local panel42 = Instance.new("Frame")
panel42.Size = UDim2.new(0, 100 + 42 * 2, 0, 20)
panel42.Position = UDim2.new(0, 168, 0, 252)
panel42.BackgroundColor3 = Color3.fromRGB(226, 234, 255)
panel42.BorderSizePixel = 0
panel42.Parent = main

local panelCorner42 = Instance.new("UICorner")
panelCorner42.CornerRadius = UDim.new(0, 6)
panelCorner42.Parent = panel42

-- Animation for Panel 42
TweenService:Create(panel42, TweenInfo.new(1 + 42 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 42 * 2, 0, 30)
}):Play()

-- Decorative Panel 43
local panel43 = Instance.new("Frame")
panel43.Size = UDim2.new(0, 100 + 43 * 2, 0, 20)
panel43.Position = UDim2.new(0, 172, 0, 258)
panel43.BackgroundColor3 = Color3.fromRGB(229, 236, 255)
panel43.BorderSizePixel = 0
panel43.Parent = main

local panelCorner43 = Instance.new("UICorner")
panelCorner43.CornerRadius = UDim.new(0, 6)
panelCorner43.Parent = panel43

-- Animation for Panel 43
TweenService:Create(panel43, TweenInfo.new(1 + 43 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 43 * 2, 0, 30)
}):Play()

-- Decorative Panel 44
local panel44 = Instance.new("Frame")
panel44.Size = UDim2.new(0, 100 + 44 * 2, 0, 20)
panel44.Position = UDim2.new(0, 176, 0, 264)
panel44.BackgroundColor3 = Color3.fromRGB(232, 238, 255)
panel44.BorderSizePixel = 0
panel44.Parent = main

local panelCorner44 = Instance.new("UICorner")
panelCorner44.CornerRadius = UDim.new(0, 6)
panelCorner44.Parent = panel44

-- Animation for Panel 44
TweenService:Create(panel44, TweenInfo.new(1 + 44 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 44 * 2, 0, 30)
}):Play()

-- Decorative Panel 45
local panel45 = Instance.new("Frame")
panel45.Size = UDim2.new(0, 100 + 45 * 2, 0, 20)
panel45.Position = UDim2.new(0, 180, 0, 270)
panel45.BackgroundColor3 = Color3.fromRGB(235, 240, 255)
panel45.BorderSizePixel = 0
panel45.Parent = main

local panelCorner45 = Instance.new("UICorner")
panelCorner45.CornerRadius = UDim.new(0, 6)
panelCorner45.Parent = panel45

-- Animation for Panel 45
TweenService:Create(panel45, TweenInfo.new(1 + 45 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 45 * 2, 0, 30)
}):Play()

-- Decorative Panel 46
local panel46 = Instance.new("Frame")
panel46.Size = UDim2.new(0, 100 + 46 * 2, 0, 20)
panel46.Position = UDim2.new(0, 184, 0, 276)
panel46.BackgroundColor3 = Color3.fromRGB(238, 242, 255)
panel46.BorderSizePixel = 0
panel46.Parent = main

local panelCorner46 = Instance.new("UICorner")
panelCorner46.CornerRadius = UDim.new(0, 6)
panelCorner46.Parent = panel46

-- Animation for Panel 46
TweenService:Create(panel46, TweenInfo.new(1 + 46 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 46 * 2, 0, 30)
}):Play()

-- Decorative Panel 47
local panel47 = Instance.new("Frame")
panel47.Size = UDim2.new(0, 100 + 47 * 2, 0, 20)
panel47.Position = UDim2.new(0, 188, 0, 282)
panel47.BackgroundColor3 = Color3.fromRGB(241, 244, 255)
panel47.BorderSizePixel = 0
panel47.Parent = main

local panelCorner47 = Instance.new("UICorner")
panelCorner47.CornerRadius = UDim.new(0, 6)
panelCorner47.Parent = panel47

-- Animation for Panel 47
TweenService:Create(panel47, TweenInfo.new(1 + 47 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 47 * 2, 0, 30)
}):Play()

-- Decorative Panel 48
local panel48 = Instance.new("Frame")
panel48.Size = UDim2.new(0, 100 + 48 * 2, 0, 20)
panel48.Position = UDim2.new(0, 192, 0, 288)
panel48.BackgroundColor3 = Color3.fromRGB(244, 246, 255)
panel48.BorderSizePixel = 0
panel48.Parent = main

local panelCorner48 = Instance.new("UICorner")
panelCorner48.CornerRadius = UDim.new(0, 6)
panelCorner48.Parent = panel48

-- Animation for Panel 48
TweenService:Create(panel48, TweenInfo.new(1 + 48 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 48 * 2, 0, 30)
}):Play()

-- Decorative Panel 49
local panel49 = Instance.new("Frame")
panel49.Size = UDim2.new(0, 100 + 49 * 2, 0, 20)
panel49.Position = UDim2.new(0, 196, 0, 294)
panel49.BackgroundColor3 = Color3.fromRGB(247, 248, 255)
panel49.BorderSizePixel = 0
panel49.Parent = main

local panelCorner49 = Instance.new("UICorner")
panelCorner49.CornerRadius = UDim.new(0, 6)
panelCorner49.Parent = panel49

-- Animation for Panel 49
TweenService:Create(panel49, TweenInfo.new(1 + 49 * 0.01, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
    Size = UDim2.new(0, 150 + 49 * 2, 0, 30)
}):Play()

-- Basic execute simulation
local function dummyExecute()
    print("Execute triggered")
end

local executeButton = Instance.new("TextButton")
executeButton.Text = "Run"
executeButton.Size = UDim2.new(0, 100, 0, 40)
executeButton.Position = UDim2.new(0.5, -50, 0.95, -45)
executeButton.BackgroundColor3 = Color3.fromRGB(80, 160, 255)
executeButton.TextColor3 = Color3.new(1, 1, 1)
executeButton.Font = Enum.Font.GothamBold
executeButton.TextSize = 20
executeButton.Parent = main

executeButton.MouseButton1Click:Connect(dummyExecute)
